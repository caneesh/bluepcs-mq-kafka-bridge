# Local Development Profile
# Uses in-memory/mock implementations - no external dependencies required

bridge:
  hdfs:
    local-base-path: ./data/hdfs
    base-path: ./data/hdfs
  ledger:
    storage-path: ./data/ledger

logging:
  level:
    com.hcsc.bridge: DEBUG
